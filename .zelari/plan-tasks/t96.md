---
kind: task
id: t96
name: M1.1 — Verifica pack nativo default ON su tree riconosciuto (già ON in codice)
status: completed
priority: high
updatedAt: "2026-09-09T08:10:08.190Z"
---
# Task t96: M1.1 — Verifica pack nativo default ON su tree riconosciuto (già ON in codice)

- Status: **completed**
- Priority: high

PIANO STALE rispetto al tree: il piano (§6) dice "pack ancora flag" ma nativePackEnabled({}) → true è già ON by default (nativeVerification.test.ts:91 'ON by default (P0.2)', ADR-0030 HARNESS-10) con resolvePackCommandsForRoot per i segnali del tree. Task = VERIFICA end-to-end, non implementazione: (1) repo JS fresco senza flag → il comando pack parte; (2) segnali non-JS (pytest.ini/pyproject/go.mod/Cargo.toml) riconosciuti, gap documentati; (3) opt-out ZELARI_VERIFY_PACK=0|off|false funziona. Acceptance: prova su fixture + nota gap; chiude M1.1 o traccia il residuo.
DONE 2026-09-10: verificato su tree — pack nativo già ON di default (ADR-0030/P0.2, nativePackEnabled({})===true, test nativeVerification.test.ts 'ON by default'). Nessun codice richiesto: il piano era stale su questo punto.
