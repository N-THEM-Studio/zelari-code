---
kind: task
id: t32
name: Pilastro B residuo — Desktop Rust client del harness server + companion client mode + steer/cancel server methods
status: completed
priority: high
updatedAt: "2026-08-29T15:56:55.838Z"
---
# Task t32: Pilastro B residuo — Desktop Rust client del harness server + companion client mode + steer/cancel server methods

- Status: **completed**
- Priority: high

Residuo del Pilastro B (t29, slice Node-side già completata e verificata: kernel appServer + --serve-harness NDJSON v2 + runOneTurn lib). Tre passi: (1) apps/desktop/src-tauri/src/lib.rs: spawn del sidecar `node bin/zelari-code.js --serve-harness` e client NDJSON stdio al posto di spawn_headless per-cwd; le 4 run parallele (MAX_PARALLEL_RUNS=4) diventano 4 sessioni session.create sullo stesso server; NO fallback silenzioso a --headless nudo se il server è giù (errore visibile all'utente); kill della UI non uccide la proof (il server sopravvive o drena le write pending). (2) companion/runManager.ts: client mode dietro ZELARI_HARNESS_SERVER=1 (default invariato: spawn). (3) harnessServer: metodi steer/cancel session-scoped (oggi suppressi via env ZELARI_SERVE_HARNESS=1 in runOneTurn). Verifica: cargo check/build desktop + test NDJSON roundtrip; DoD = criterio di chiusura 2 completo del doc HARNESS-10.
Completato via tentacoli paralleli (Node + Rust disgiunti). Node: sessionControl.ts (ALS, 92 LOC), session.steer/cancel su protocollo v2 (unknown_session, already_finished), harnessClient.ts, companion client mode ZELARI_HARNESS_SERVER=1 (default spawn byte-identico, testato). Rust: harness_sidecar.rs (936 LOC) + lib.rs (-262 netto), 1 sidecar long-lived, 4 run=4 sessioni, restart backoff, NO fallback --headless, graceful proof drain 8s; cargo check 0 warning, cargo test --lib 10/10. Committato in d725b80.
