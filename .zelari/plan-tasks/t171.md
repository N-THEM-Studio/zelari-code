---
kind: task
id: t171
name: "B7: fork sessioni da turn boundary (Store.Fork-style)"
status: completed
priority: medium
updatedAt: "2026-09-23T11:10:00.000Z"
---
# Task t171: B7: fork sessioni da turn boundary (Store.Fork-style)

- Status: **completed**
- Priority: medium

Vedi .zelari/docs/2026-09-21-piano-roi-steal-unreal-agent.md §B7. Fork da un turn boundary: storia ereditata, stato transiente azzerato; sessione figlia con seq/lineage coerenti col contratto spine v1 (ADR-0021); replay indipendente dalla madre. Rispettare l'ownership lock single-writer della spine. Gemello: worktree Kraken.

---

**Completato** 2026-09-23 — implementazione già esistente (`packages/core/src/session/lineage.ts`), test di copertura aggiunti.

L'infrastruttura `forkSession(store, parentId, {fromSeq})` esisteva già:
- Copia eventi con `seq <= fromSeq` in una nuova sessione (ri-sequenziati 1..n)
- Appende `session.forked {parentSessionId, parentSeq}` per il lineage
- `lineageOf(store, id)` cammina i marcatori `session.forked` per ricostruire la catena
- Rispetta l'ownership lock single-writer (ogni sessione ha il proprio writer)
- Replay della figlia indipendente dalla madre (projection separata)

Test aggiunti in `packages/core/src/session/lineage.test.ts`:
- Fork al turn boundary preserva tool.call + tool.result + assistant.message (seq coerenti)
- Replay della figlia indipendente dalla madre (eventi divergenti non si contaminano)
- Export della figlia indipendente (session ID diverso, marcatori corretti)

Acceptance:
1. ✅ Fork da turn boundary → sessione figlia con seq/lineage coerenti col contratto spine v1
2. ✅ Replay indipendente dalla madre; export separato
