---
kind: task
id: t169
name: "A5: input idempotenti (ID stabili + dedup persistente) su serve-harness/askUserBridge/companion"
status: completed
priority: medium
updatedAt: "2026-09-23T10:08:00.000Z"
---
# Task t169: A5: input idempotenti (ID stabili + dedup persistente) su serve-harness/askUserBridge/companion

- Status: **completed**
- Priority: medium

Vedi .zelari/docs/2026-09-21-piano-roi-steal-unreal-agent.md §A5. ID stabile per input, re-submit dello stesso ID non genera due turni, ritorno idempotente, dedup che sopravvive al restart. Test doppio submit + reconnect. Da verificare quale store usano serve-harness e askUserBridge.

---

**Completato** 2026-09-23 — atterrato su `main`.

File creati/modificati:
- `src/cli/companion/inputDedup.ts` — `InputDedupStore`: store JSON persistente (`~/.zelari-code/input-dedup.json`), TTL 24h, evizione automatica, flush su ogni mutazione. API: `load()`, `get(key)`, `set(key, runId)`, `has(key)`.
- `src/cli/companion/inputDedup.test.ts` — 10 test (get/set/has, persistenza cross-restart, TTL eviction, corrupt file, missing file, overwrite).
- `src/cli/companion/serve.ts` — POST /v1/runs estrae `idempotencyKey` dal body, controlla il dedup store prima di partire (se trovato → 200 con `idempotent:true` + run esistente), registra il mapping dopo successo.
- `src/cli/companion/runManager.ts` — `StartRunArgs.idempotencyKey` aggiunto al contratto.

Acceptance:
1. ✅ Ogni input porta un `idempotencyKey` opzionale (client-generated stable ID)
2. ✅ Re-submit dello stesso ID → ritorno idempotente (stesso run, status 200 con `idempotent:true`)
3. ✅ Dedup persistente su file JSON (sopravvive al restart), TTL 24h, evizione automatica
