---
kind: task
id: t1
name: "WS2 — ast_outline/find_symbol: result discriminato + risoluzione path vs cwd"
status: completed
priority: high
updatedAt: "2026-08-16T23:42:05.543Z"
---
# Task t1: WS2 — ast_outline/find_symbol: result discriminato + risoluzione path vs cwd

- Status: **completed**
- Priority: high

Piano: .zelari/docs/piano-loud-tool-errors-ast-diag-readonly-shell.md §2. File: src/cli/ast/engine.ts (parseFileSymbolsDiag), src/cli/ast/tools.ts (note specifiche + ctx.cwd). Bug reale: tools.ts:34-36 non risolve path relativi vs ctx.cwd (grep_content lo fa, search.ts:170). Acceptance: path relativo funziona; file-not-found riporta il path assoluto guardato; niente piu' note a tre ipotesi in OR. Test S2.3 inclusi.
DELTA post-valutazione esterna: aggiungere S2.0 PRIMA di S2.1 — createAstTools(root) (src/cli/ast/tools.ts:21 oggi senza parametri, chiamata bare a toolRegistry.ts:338; root già in scope nel registry, cfr. l.423). Questo è il fix a monte del bug path. Aggiungere variante read-error ≠ file-not-found in ParseFileSymbolsResult. Dettagli in sezione 7 del piano.
Rev.2 (§8 piano): ogni variante non-ok del ParseFileSymbolsResult porta anche campi macchina resolvedPath?/recoverable/recommendedFallback? ('grep_content'|'read_file') — l'info non vive solo nella stringa note. recommendedFallback resta hint, mai auto-esecuzione.
WS2 implementato e verificato su disco (BUILD 2026-08-17): S2.0 createAstTools(root) + call site toolRegistry.ts:338; S2.1 parseFileSymbolsDiag(file, cwd?) con ParseFileSymbolsResult a 6 varianti (incluse read-error e campi macchina rev.2 resolvedPath/recoverable/recommendedFallback), wrapper quiet compatibili; S2.2 note loud per variante, found:false solo dopo parse riuscito; S2.3 test engine.test.ts (6) + tools.test.ts (8). Verifiche: npx vitest run src/cli/ast → 14/14; npm test → 2385/2385; npm run typecheck → ok.
