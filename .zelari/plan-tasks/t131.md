---
kind: task
id: t131
name: "M2.3 — Determinismo trailing: audit caller + quantizzazione/memo render"
status: completed
priority: medium
updatedAt: "2026-09-19T22:09:00.837Z"
---
# Task t131: M2.3 — Determinismo trailing: audit caller + quantizzazione/memo render

- Status: **completed**
- Priority: medium

Dipende da M2.1 (agisce sul trailing). systemPromptBuilder.ts è già puro (verificato: nessun Date.now/random/uptime; Map/Set deterministici dato l'input) → il churn viene dai CALLER che compongono workspaceContext/ragContext/durableStateContext. Passi: (1) grep Date.now()/new Date(/randomUUID/Math.random nei composer del volatile e quantizzare (timestamp→minuto) o rimuovere; (2) memo/fingerprint per turno: input invariati → riusare stringa precedente byte-identica. Acceptance: unit test due render stessi input → stringa identica; nessun timestamp non quantizzato nel volatile (allowlist esplicita). Piano: sezione M2.3
Audit: nessun timestamp non quantizzato nel builder (grep Date.now/toISOString in agents/: solo telemetria eventi council, fuori dal prompt). Determinismo garantito da memo lastTrailingRender in wrapTrailingContext. Churn residuo workspace/RAG accettato per design (motivo stesso del trailing).
