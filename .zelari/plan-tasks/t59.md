---
kind: task
id: t59
name: "cli: staleness check a session start (gitLogSince su globs dei completati)"
status: pending
priority: high
updatedAt: "2026-08-31T01:32:53.748Z"
---
# Task t59: cli: staleness check a session start (gitLogSince su globs dei completati)

- Status: **pending**
- Priority: high

Nuovo export gitLogSince(cwd, sinceIso, paths): Promise<string[]> in src/cli/gitOps.ts — wrapper git() privato già esiste (gitOps.ts:56-67, never-throw); oggi NON esiste nessun wrapper git log. Al bootstrap sessione (stessi punti del TaskTouchGuard, dopo SessionSpineMirror.adopt / openHeadlessSpine — sessionSpine.ts:267-285, headlessSpine.ts:106-135 — fire-and-forget, MAI bloccante): per ogni task completed con files e completedAt più vecchio di soglia (default 24h, env tunabile ZELARI_TASK_STALE_HOURS), se git log --oneline --since=<completedAt> -- <files> restituisce righe -> flag 'stale' + appendNote + radio kind 'task_stale'. Skip silenzioso se !isGitRepo(cwd). Spirito ADR 0023: advisory-only, unknown non blocca nulla. ACCETTAZIONE: test con repo git reale in os.tmpdir() stile tests/unit/cli-gitOps.test.ts:16-40 (commit dopo completedAt -> stale; nessun commit -> nulla; non-repo -> skip).
