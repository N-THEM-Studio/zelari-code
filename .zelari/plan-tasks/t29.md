---
kind: task
id: t29
name: "2.19 — Pilastro B: App Server (runOneTurn lib + harnessServer + desktop/companion client, no fork)"
status: completed
priority: high
updatedAt: "2026-08-29T15:57:02.779Z"
---
# Task t29: 2.19 — Pilastro B: App Server (runOneTurn lib + harnessServer + desktop/companion client, no fork)

- Status: **completed**
- Priority: high

lobal (policyLoadMode.ts :69-75); HEADLESS_PROTOCOL_VERSION=2 vive in src/cli/headless/protocol.ts :13. Passi: (1) estrarre runOneTurn da runHeadless.ts (refactor no behavior-change, test:session verdi); (2) packages/core/src/harness/appServer.ts kernel long-lived senza React: N sessioni, 1 ToolRegistry/sessione, 1 LspManager per workspace, writer CompletionProof, factory osJail, trasporto stdio NDJSON (protocollo resta v2); (3) src/cli/serve/harnessServer.ts estende companion/serve senza rompere /v1/runs; runManager diventa client; (4) Desktop lib.rs sidecar client, NO fallback silenzioso a --headless nudo se server giù; le 4 run = 4 sessioni; (5) --headless CI resta client in-process dello stesso kernel. DoD: criterio di chiusura 2 del doc; test kill UI non uccide la prova; secondo run riusa policy/LSP.
Slice Node-side COMPLETATA e verificata dal lead (typecheck 0, 6/6 test nuovi, 79 test regressione headless+t28 verdi): runOneTurn estratto in src/cli/headless/runOneTurn.ts (refactor meccanico, runHeadless.ts 1962→1251 LOC), kernel packages/core/src/harness/appServer.ts(+Types) (sessioni, servizi per-workspace refcounted: LspManager/policyCache/proofWriter riusati, proof-write mai cancellata dal disconnect), entry --serve-harness in src/cli/serve/harnessServer.ts + main.ts (NDJSON stdio, protocollo v2 invariato, errori tipizzati, bad_json non crasha). RESIDUO per criterio di chiusura 2 completo: client Rust Desktop (lib.rs sidecar, no fallback silenzioso, 4 run = 4 sessioni), companion runManager in client mode, steer/cancel come metodi server → registrato in t32.
Chiusura definitiva: il residuo (t32) è completato e verificato — Desktop sidecar client + companion client mode + steer/cancel. Criterio di chiusura 2 del doc HARNESS-10 soddisfatto: UI kill non uccide la proof (graceful drain 8s nel sidecar, dispose() drena), secondo run riusa servizi per-workspace (refcount), nessun fallback silenzioso a --headless. Commit: b90c9a9 (kernel+lib+server) + d725b80 (t32).
