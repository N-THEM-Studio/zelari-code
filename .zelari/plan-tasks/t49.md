---
kind: task
id: t49
name: "2.17.1-B — fix(harness): skip build-liveness recovery when the resource gate is exhausted"
status: completed
priority: high
updatedAt: "2026-08-30T19:48:57.830Z"
---
# Task t49: 2.17.1-B — fix(harness): skip build-liveness recovery when the resource gate is exhausted

- Status: **completed**
- Priority: high

Ricetta: TASK_resource-gate-hardening_2.17.1.md (Task B; rinumerato da t48→t49). File prod: packages/core/src/core/AgentHarness.ts, planBuildRecovery :993-1005 (const annidato, non metodo) — guard DOPO il check recoveries-cap e PRIMA di this.buildProgress.recoveries += 1: if (!this.checkToolCallGate('edit_file', {}).allowed) return null; con commento 2.17.1 (sonda edit_file = DEFAULT_ESSENTIAL_TOOLS in src/cli/budget/budgetRuntime.ts:46; negato ⇒ hard limit o gate fail-closed; gate assente ⇒ {allowed:true} ⇒ inerte). Nessun nuovo evento (scelta deliberata: spine ha già resource.limit_reached/overrun adiacenti). File test: packages/core/src/core/agentHarnessBuildLiveness.test.ts — 1 test nuovo 'skips liveness recovery when the gate denies mutations (hard limit)' specchiando il test :86-115 (provider zero-write stop, calls counter, attempts[]): gate nega sempre {allowed:false, reason:'Resource exhausted: …', hardLimit:true}, buildLiveness {mutationRequired:true, maxRecoveries:2}; assert calls===1, attempts===[] , evento {type:'error',severity:'fatal',code:'build_liveness_stalled'}, agent_end reason 'error', harness.getMessages() (accessor :460-461) senza '[build-liveness]' (prompt definito :171-175). Suite esistente SENZA gate = controllo a regime (guard inerte). Ordine: DOPO t48/A. Verify: npx vitest run packages/core/src/core/agentHarnessBuildLiveness.test.ts packages/core/src/core/agentHarnessToolGate.test.ts + npm run typecheck. Commit atomico singolo. Nota: dipendenza da A solo editoriale — la sonda legge solo .allowed, non hardLimit.
Verificato su disco 2026-08-31: già in main come commit ce8d20a (pre-release 2.18.1). Guardia in planBuildRecovery AgentHarness.ts:1037 (checkToolCallGate('edit_file')), test in agentHarnessBuildLiveness.test.ts (:117). Nessun lavoro residuo.
