---
kind: task
id: t3
name: WS3 — bash_readonly in plan/readOnly (validazione statica + spawn senza shell)
status: completed
priority: medium
updatedAt: "2026-08-17T00:07:53.149Z"
---
# Task t3: WS3 — bash_readonly in plan/readOnly (validazione statica + spawn senza shell)

- Status: **completed**
- Priority: medium

ECT_UNSAFE_EXTRA_OPERATIONS solo in backlog.
DELTA FINALE (review round 3, approvata dal proprietario): nuova slice S3.5 "typecheck artifact safety" = condizione di DONE di WS3. (1) Builder typecheck passa sempre --tsBuildInfoFile <tmp>/zelari-inspect/<hash> (redirect PRIMARIO; --incremental false è solo fallback — composite forza incremental e disattivarlo rischia errore TS sul fixture). (2) Guard pre/post: git status --porcelain + scan **/*.tsbuildinfo; delta → status degraded + artifactsWritten + cleanup. (3) Fixture: plain / incremental+tsBuildInfoFile-esplicito-in-workspace (CLI override deve vincere) / composite (packages/core è il caso reale); assert git status pulito + zero tsbuildinfo nuovi. (4) TS-level refusal su composite → status unsupported_project_shape loud. (5) typecheck = inspectionClass project-code-execution. Backlog: classe network-inspection per npm_view/npm_outdated (non blocca). Dettagli: .zelari/docs/piano-loud-tool-errors-ast-diag-readonly-shell.md §9.
WS3 implementato e verificato su disco (BUILD slice 3, 2026-08-17): S3.1/S3.2/S3.3/S3.4/S3.5 completi. Nuovi file: src/cli/tools/inspectCommand.ts (union discriminata su operation, argv interno, spawn shell:false, cap 8KB, inspectionClass, timeout 85s); src/cli/tools/inspectTypecheckSafety.ts (fingerprint git-status+scan tsbuildinfo, diff, cleanup, classifyTypecheckRefusal); src/cli/tools/inspectCommand.test.ts (23 test: builder tabellare, spawn timeout/ENOENT, guard units, fixture S3.5 plain/incremental-explicit/composite con redirect CLI vincente, registry gating readOnly/planMode/full/kill-switch). Modificati: toolRegistry.ts (registrazione readOnly+ZELARI_INSPECT_COMMAND!=0 dopo la dichiarazione tools), useChatTurn.ts banner PLAN, docs/TOOLS.md sezione inspect_command, tests/unit/cli-lsp.test.ts (test WS4 stale aggiornato alla nuova semantica read-only — era la causa del verification ok=false). Verifiche: vitest inspectCommand 23/23; npm test 2423/2423; npm run typecheck ok; smoke bin ok.
