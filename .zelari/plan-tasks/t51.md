---
kind: task
id: t51
name: "T4-S2 — wiring noteBudgetProjection: seam unico modelContextBuilder (+holder dai call site)"
status: completed
priority: high
updatedAt: "2026-08-30T21:27:02.466Z"
---
# Task t51: T4-S2 — wiring noteBudgetProjection: seam unico modelContextBuilder (+holder dai call site)

- Status: **completed**
- Priority: high

Piano T4 (ADR-0032 occupancy budget → spine), slice 2 di 6. Dipende da t50 (typecheck).
Scope: wiring budget-path con seam UNICO, non 5 call site. Punto naturale: src/cli/budget/modelContextBuilder.ts dopo sessionHistory(input.session) (:115) — copre tutti i call site produttivi: runOneTurn.ts:483, useChatTurn.ts:454 (chat TUI) + :1489 (council), runHeadless.ts:828 (council) + :1077 (mission).
PUNTO APERTO da verificare in BUILD: ModelContextBuilderInput espone già un seam note (handle spine / late-binding holder)? Se sì usarlo; se no aggiungere campo opzionale (es. budgetNoteHandle) e valorizzarlo dai call site elencati. BudgetPolicy (occupancy/estimatedHistoryTokens/contextLimit/contextPressureTokens) è già sulla input surface.
Composizione: noteBudgetProjection(handle, recordFromBudget(budget, thresholdsFor(model, provider))) — tutto never-throw (contextProjection.ts:72-90 + :45-65).
Payload budget-side MAI contextChars/returnedCount (restano memory-side, spineTelemetry.ts:38-48). SCHEMA_VERSION resta 1.
Acceptance: unit test con fake handle (nota context.projection con occupancy+policy; NESSUNA nota se handle assente = backward-compat); test leggero e2e runOneTurn fake → spine contiene nota budget-side; tsc 0. Commit atomico.
DONE 2026-08-31: seam budgetNoteHandle su ModelContextBuilderInput (modelContextBuilder.ts:93) + nota budgetNoteProjection(recordFromBudget(budget, thresholdsFor(model,provider))) pre-return, try/catch silente. NOTA: useChatTurn.ts non esiste più — scope TUI inattuabile, i call site reali sono runOneTurn:498 + runHeadless:841/1092 + test. Commit 66da224.
