---
kind: task
id: t108
name: "TUI: thinking live opt-in (/thinking + buffer parallelo in useChatTurn)"
status: completed
priority: high
updatedAt: "2026-09-16T23:30:20.985Z"
---
# Task t108: TUI: thinking live opt-in (/thinking + buffer parallelo in useChatTurn)

- Status: **completed**
- Priority: high

Innesti: (1) useChatTurn.ts ~1149–1157 — su evento thinking_delta accumulare in thinkingContent parallelo a streamContent (NON passare da cleanAgentContent, o usare scrub separato); (2) chatState.ts LiveState 59–110 — campo thinking?: string + estensione setStreaming; (3) LiveRegion.tsx StreamingTail 107–132 — coda dim/italic clampata (~3 righe) sopra/sotto lo stream; (4) slash command /thinking on|off persistito nei prefs. Vincoli: thinking MAI nel path finalizeStreaming→Static; spine resta senza thinking (sessionSpine.ts:199 droppa per design); nessun nuovo BrainEvent (thinking_delta esiste già in packages/core/src/shared/events.ts:24).
Implementato su disco (verificato: tsc --noEmit exit 0; test cli-liveRegion 7/7, userSettings 7/7, cli-useChatTurn verdi). Percorsi REALI (il piano citava src/cli/tui/* inesistente): chatState.ts (LiveState.thinking + setLiveThinking + LIVE_THINKING_TAIL_CHARS=600), useChatTurn.ts dispatchPrompt (buffer thinkingContent, handler thinking_delta @100ms, clear al primo message_delta e a message_end), LiveRegion.tsx (blocco "◌ reasoning · ~N tok" dim/italic clamp 10 righe), userSettings.ts (leaf showThinking default false + ZELARI_SHOW_THINKING). Gate ottieni via env/config, NON via comando /thinking (rimandato: dispatch slash non toccata). Council path (riga 1887+) NON coperto in questa slice.
