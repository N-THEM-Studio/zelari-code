---
kind: task
id: t30
name: "2.20 — Pilastro C: ExtensionAPI seam (registerTool via wrapWithPermissions + onPreToolUse, sandboxedFs)"
status: completed
priority: medium
updatedAt: "2026-08-29T13:26:34.635Z"
---
# Task t30: 2.20 — Pilastro C: ExtensionAPI seam (registerTool via wrapWithPermissions + onPreToolUse, sandboxedFs)

- Status: **completed**
- Priority: medium

Verificato: plugins/registry.ts è catalogo binari (eslint, ruff, playwright, typescript-language-server, pyright) — non ExtensionAPI (nota: doc diceva tsserver, id reale typescript-language-server). ADR-0022 rifiuta framework plugin generico: ExtensionAPI = seam, non framework/marketplace, non eval di JS dal prompt; MCP resta tool remoto. File: packages/core/src/harness/extensionApi.ts (ExtensionHost: registerTool con stesso Zod + permissions OBBLIGATORIAMENTE via wrapWithPermissions, onPreToolUse, sandboxedFs only); load da ~/.zelari-code/extensions sempre + .zelari/extensions solo se trusted (folderTrust.ts : gates solo hooks+MCP, mai policy.json); extensions.lock opzionale con hash; estensione di esempio in-tree. ContractCompiler resta l'ultimo intersect (sopra nessuno). DoD: criterio di chiusura 3; test estensione che tenta scavalcare policy ⇒ negata dal wrap.
Completato via tentacolo + verifica lead (typecheck 0, 18/18 test nuovi, 52/52 combinati). extensionApiTypes/extensionApi in core (host ristretto: fs sandboxed only, niente process/net), loader CLI (global sempre, progetto solo se trusted, lock sha256 pre-import, strict=batch reject), wiring in toolRegistry via STESSO withPerm dei builtin (ContractCompiler resta ultimo intersect), PreToolUse estensioni con semantica t22, esempio in-tree examples/extensions/echo-tool. TUI wiring lasciato come extension point opzionale (headless copre il path completo). Kill switch ZELARI_EXTENSIONS=0.
