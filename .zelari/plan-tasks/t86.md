---
kind: task
id: t86
name: "B5 — Desktop: stessi chip + doctor-first se rosso; NIENTE card Evolution"
status: completed
priority: medium
updatedAt: "2026-09-04T23:54:28.606Z"
---
# Task t86: B5 — Desktop: stessi chip + doctor-first se rosso; NIENTE card Evolution

- Status: **completed**
- Priority: medium

aseToggle in topbar App.tsx:3030-3035 (plan/build con hint permessi, PhaseToggle.tsx), VerificationStatusCard App.tsx:3290 → NON toccare. (2) Zero card Evolution nel desktop (grep 0 match) — congelamento rispettato. (3) Gap reale = doctor-first: gate esistente showCliSetup (App.tsx:2802 `!setupDismissed && !cliStatusLoading && cli !== null && !cli.ok`) guarda solo presenza/versione CLI via Rust; ZERO riferimenti a doctor in src-tauri (*.rs). SPEC (slice sottile, ~3 file): [a] CLI: flag `--doctor --json` in src/cli/main.ts che stampa JSON di collectDoctorReport() (export già aggiunto in 2.31, doctor.ts) — exit 0 sempre, superficie additiva, stesso contratto ADR-0034; [b] Rust: nuovo comando `cli_doctor_check` in src-tauri (spawn `zelari-code --doctor --json`, serde-parse, ritorna {ok, firstRed:{label,commandHint}}); [c] App.tsx: estendere il gate — se cli.ok MA doctor rosso → schermata doctor-first col primo rosso + comando esatto + "Continue anyway" esplicito (specchio di runFirstRunDoctorGate in src/cli/main.ts, aggiunto in 2.31). Verify: tsc desktop + `npm run typecheck` root + smoke `zelari-code --doctor --json | ConvertFrom-Json`.
COMPLETED 2026-09-06 in commit 494b8ce (branch feat/b5-desktop-doctor, pushed): [a] CLI --doctor --json (main.ts, additive; exit code = health); [b] lib.rs cli_doctor_check (spawn --doctor --json, parse stdout-first like test_ssh_target; registered in generate_handler); [c] DoctorGate.tsx + App.tsx gate (refreshCli runs doctor when cli.ok; first red blocks chat; explicit "Continue anyway (declared)"; re-run button); agentClient.getCliDoctorCheck wrapper. VERIFIED: desktop tsc --noEmit exit 0; verify-versions coherent INCLUDING new check 8 (Cargo.lock structural gate — 2.31.0 lesson codified: every [[package]] needs name+version, zelari-desktop exactly once). NOT VERIFIED locally: cargo check blocked by permission layer on both bash and exec_process channels — Rust validated by pattern-mirror of test_ssh_target + CI desktop at next tag.
