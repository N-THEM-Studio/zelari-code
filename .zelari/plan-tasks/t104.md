---
kind: task
id: t104
name: B1 — Anticipare setBusy(true) in dispatchPrompt per eliminare il gap echo→spinner
status: completed
priority: high
updatedAt: "2026-09-16T22:58:59.743Z"
---
# Task t104: B1 — Anticipare setBusy(true) in dispatchPrompt per eliminare il gap echo→spinner

- Status: **completed**
- Priority: high

Slice 1 / B1. PRIMA: useChatTurn.ts:324 setBusy(true) arriva DOPO providerFromEnv + dynamic import processSessionBudget + budget.isHold() check; l'utente vede solo l'echo del prompt per 50-300ms prima del WorkingIndicator. DOPO: setBusy(true) diventa il PRIMO step di dispatchPrompt (subito dopo appendUser), PRIMA di qualsiasi await di setup. I check che falliscono (key mancante, budget HOLD) emettono appendSystem di errore e poi setBusy(false) per ripristinare lo stato coerente. I check post-setBusy (beginResourceTurn, buildModelContext, spine.userMessage, dynamic imports council) restano dove sono — non sono il collo di bottiglia percepito perché il WorkingIndicator è già visibile. NO qualità ridotta (toggle UX pura), NO modelli limitati. Out of scope per slice 1: dispatchCouncilPrompt (riga 1650), runAgentMissionSlice (riga 2734) — stesso pattern ma flussi diversi, backlog P2 perf.
