---
kind: task
id: t170
name: "B6: RemoteJob anti-tamper plan per esecuzione remota SSH"
status: completed
priority: high
updatedAt: "2026-09-23T11:05:00.000Z"
---
# Task t170: B6: RemoteJob anti-tamper plan per esecuzione remota SSH

- Status: **completed**
- Priority: high

Vedi .zelari/docs/2026-09-21-piano-roi-steal-unreal-agent.md §B6. RemoteJobPlan{Type, Version, Data} con vincolo su tipo|versione|max-output verificato prima dell'esecuzione; mutazione ⇒ rifiuto con errore strutturato + test del rifiuto. Target: esecuzione remota SSH prod-vps e sandbox tentacoli. NON estendere l'allowlist SSH.

---

**Completato** 2026-09-23 — atterrato su `main`.

File creati/modificati:
- `src/cli/ssh/remoteJobPlan.ts` — `RemoteJobPlan` con `type`, `version`, `data`, `maxOutputBytes`, `seal` (SHA-256). Funzioni: `createPlan(input)` (crea + seal), `verifyPlan(plan)` (integrity + constraint check → `PlanVerificationResult`), `checkPlanConstraints(plan, opts)` (type allowlist + output limit). Serializzazione canonica deterministica per il seal.
- `src/cli/ssh/remoteJobPlan.test.ts` — 22 test (create, seal determinismo, round-trip JSON, tamper detection su type/data/maxOutput/seal, constraint check su type allowlist e output limit).
- `src/cli/ssh/tools.ts` — `ssh_run` accetta `plan` opzionale nel input schema; se fornito, `verifyPlan` + `checkPlanConstraints` prima dell'esecuzione. Allowlist SSH NON estesa.

Acceptance:
1. ✅ Piano serializzato con vincolo su type|version|maxOutputBytes verificato prima dell'esecuzione; mutazione → rifiuto con errore strutturato (`seal_mismatch`, `constraint_violated`)
2. ✅ Test del rifiuto (22 test: tampered type/data/maxOutput/seal → non eseguito)
