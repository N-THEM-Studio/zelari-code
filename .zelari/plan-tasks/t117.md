---
kind: task
id: t117
name: "C2 — ACP front door: `zelari-code acp` server stdio (Agent Client Protocol) per integrazione editor"
status: completed
priority: medium
updatedAt: "2026-09-19T12:01:15.686Z"
---
# Task t117: C2 — ACP front door: `zelari-code acp` server stdio (Agent Client Protocol) per integrazione editor

- Status: **completed**
- Priority: medium

Fase C2. Da mcode packages/tui/src/acp/ + run-acp-command.ts (@agentclientprotocol/sdk). Terza front door dopo TUI e headless: subcommand `zelari-code acp` (server stdio) che riusa il core agente già servito da runHeadless.ts (AgentHarness) senza duplicare logica. Scope v1: initialize, session/new, session/prompt con streaming (agent_message_chunk, tool_call/tool_call_update), cancel, request_permission bridgato al nostro permission broker esistente. Dipendenza: @agentclientprotocol/sdk — VERIFICARE peso bundle e licenza MIT prima di aggiungerla (convenzione zero heavy deps); in caso negativo fallback: subset JSON-RPC stdio implementato a mano (~200 LOC). Acceptance: integration smoke con client finto su stdio (initialize handshake + un prompt echo); nessuna regressione TUI/headless; docs TOOLS.md + sezione integrazioni editor (Zed/Neovim); typecheck+test+build verdi.
COMPLETATO (verificato su disco): src/cli/acp/ (framing/protocol/server/eventMap/turnAdapter/command, 7 file test), main.ts wired (+38), ZERO nuove dep (JSON-RPC a mano, fallback del piano). Smoke funzionale dal bundle: `Content-Length` framing, initialize → risposta protocolVersion 1, exit 0 su EOF.
