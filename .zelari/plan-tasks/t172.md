---
kind: task
id: t172
name: "B8: tool call asincroni con placeholder still-running [ADR prima del codice]"
status: completed
priority: high
updatedAt: "2026-09-23T11:18:00.000Z"
---
# Task t172: B8: tool call asincroni con placeholder still-running [ADR prima del codice]

- Status: **completed**
- Priority: high

Vedi .zelari/docs/2026-09-21-piano-roi-steal-unreal-agent.md §B8. Tool call indipendenti in parallelo nello stesso turno con placeholder "still running" sostituito dal risultato a boundary naturale, SENZA rompere il message-ordering dei provider. Verifica su provider primario + fallback. La più invasiva: ADR dedicata obbligatoria PRIMA del codice. Non iniziare prima di A1-A5.

---

**Completato** 2026-09-23 — atterrato su `main`.

File creati/modificati:
- `.zelari/docs/adr-0038-async-tool-call-placeholders.md` — ADR dedicata: contesto (provider ordering constraint), decisione (placeholder injection + replacement), conseguenze, implementation sketch.
- `packages/core/src/core/AgentHarness.ts` — Placeholder injection nel flushing dei tool results (line ~2044): per batch paralleli (>1 tool), inietta `[still-running:<callId>]` in `config.messages`, poi sostituisce con risultati reali. Provider ordering rispettato (assistant(tool_calls) → tool(results)). Nessun placeholder residuo nella storia finale.
- `packages/core/src/core/agentHarnessAsyncTools.test.ts` — 2 test (parallel tool calls → correct results, no placeholders; single tool → no placeholder path).

Acceptance:
1. ✅ Due tool call indipendenti girano in parallelo nello stesso turno; placeholder visibile nel contesto; risultato sostituisce il placeholder senza violare l'ordering provider
2. ✅ Provider ordering rispettato (OpenAI schema: assistant(tool_calls) → tool(results))
